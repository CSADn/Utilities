<Window x:Class="DashMPDPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:models="clr-namespace:DashMPDPlayer.Models"
        Title="IPTV DASH Player" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#1c1c1c" Foreground="#e0e0e0"
        FontFamily="Segoe UI" FontSize="13"
        TextElement.Foreground="#e0e0e0"
        TextOptions.TextFormattingMode="Ideal">
    <Window.Resources>
        <SolidColorBrush x:Key="BgDark" Color="#1c1c1c"/>
        <SolidColorBrush x:Key="BgMedium" Color="#2a2a2a"/>
        <SolidColorBrush x:Key="BgLight" Color="#333"/>
        <SolidColorBrush x:Key="BgInput" Color="#181818"/>
        <SolidColorBrush x:Key="FgPrimary" Color="#e0e0e0"/>
        <SolidColorBrush x:Key="FgSecondary" Color="#999"/>
        <SolidColorBrush x:Key="FgMuted" Color="#666"/>
        <SolidColorBrush x:Key="BorderColor" Color="#3a3a3a"/>
        <SolidColorBrush x:Key="Accent" Color="#005a9e"/>
        <SolidColorBrush x:Key="AccentHover" Color="#1a6db5"/>
        <SolidColorBrush x:Key="SelectionBg" Color="#005a9e"/>
        <SolidColorBrush x:Key="HoverBg" Color="#333"/>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="{StaticResource BorderColor}"/>
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="Height" Value="1"/>
        </Style>

        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Background" Value="#181818"/>
            <Setter Property="BorderBrush" Value="#3a3a3a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#999"/>
            <Setter Property="CaretBrush" Value="#e0e0e0"/>
        </Style>

        <ControlTemplate x:Key="MenuTopLevelHeader" TargetType="MenuItem">
            <Grid SnapsToDevicePixels="True">
                <Border x:Name="border" Background="Transparent" Padding="8,4">
                    <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center"
                                      TextElement.Foreground="#e0e0e0"/>
                </Border>
                <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsSubmenuOpen}"
                       AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                    <Border Background="#2a2a2a" BorderBrush="#3a3a3a" BorderThickness="1" Padding="2">
                        <ScrollViewer Background="#2a2a2a" BorderThickness="0"
                                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#333"/>
                </Trigger>
                <Trigger Property="IsSubmenuOpen" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#2a2a2a"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <ControlTemplate x:Key="MenuSubmenuItem" TargetType="MenuItem">
            <Border x:Name="border" Background="#2a2a2a" Padding="6,3" SnapsToDevicePixels="True"
                    BorderBrush="#3a3a3a" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="MenuIcon"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ContentPresenter x:Name="icon" ContentSource="Icon" Margin="2,0,6,0" VerticalAlignment="Center" Width="16" Height="16"/>
                    <Border x:Name="check" Width="16" Height="16" Margin="2,0,6,0" Visibility="Collapsed"
                            BorderBrush="#888" BorderThickness="1" Background="Transparent">
                        <TextBlock x:Name="checkMark" Text="✓" Foreground="#e0e0e0"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontSize="12" Visibility="Collapsed"/>
                    </Border>
                    <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True"
                                      VerticalAlignment="Center" TextElement.Foreground="#e0e0e0"/>
                    <TextBlock Grid.Column="2" x:Name="gesture" Margin="12,0,4,0" VerticalAlignment="Center"
                               TextElement.Foreground="#999" Text="{TemplateBinding InputGestureText}"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#005a9e"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                </Trigger>
                <Trigger Property="IsCheckable" Value="True">
                    <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="icon" Property="Visibility" Value="Collapsed"/>
                </Trigger>
                <Trigger Property="Icon" Value="{x:Null}">
                    <Setter TargetName="icon" Property="Visibility" Value="Collapsed"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="MenuItem">
            <Setter Property="Template" Value="{StaticResource MenuSubmenuItem}"/>
            <Style.Triggers>
                <Trigger Property="Role" Value="TopLevelHeader">
                    <Setter Property="Template" Value="{StaticResource MenuTopLevelHeader}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <ControlTemplate x:Key="TreeViewItemTemplate" TargetType="TreeViewItem">
            <Grid SnapsToDevicePixels="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ToggleButton x:Name="Expander" ClickMode="Press"
                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                  Width="16" Height="16" Margin="2,0" Background="Transparent" RenderTransformOrigin="0.5,0.5">
                        <ToggleButton.LayoutTransform>
                            <RotateTransform Angle="0"/>
                        </ToggleButton.LayoutTransform>
                        <ToggleButton.Style>
                            <Style TargetType="ToggleButton">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent" Width="16" Height="16">
                                                <TextBlock Text="&#x25B6;" FontSize="8" Foreground="#999"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter Property="LayoutTransform">
                                            <Setter.Value>
                                                <RotateTransform Angle="90"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ToggleButton.Style>
                    </ToggleButton>
                    <Border x:Name="Bd" Grid.Column="1" Background="Transparent"
                            Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True"
                            TextElement.Foreground="#e0e0e0">
                        <ContentPresenter ContentSource="Header" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                </Grid>
                <ItemsPresenter x:Name="ItemsHost" Grid.Row="1" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Visibility="Collapsed"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="HasItems" Value="False">
                    <Setter TargetName="Expander" Property="Visibility" Value="Collapsed"/>
                </Trigger>
                <Trigger Property="IsExpanded" Value="True">
                    <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter TargetName="Bd" Property="Background" Value="#005a9e"/>
                    <Setter TargetName="Bd" Property="TextElement.Foreground" Value="#fff"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected" Value="True"/>
                        <Condition Property="Selector.IsSelectionActive" Value="False"/>
                    </MultiTrigger.Conditions>
                    <Setter TargetName="Bd" Property="Background" Value="#333"/>
                    <Setter TargetName="Bd" Property="TextElement.Foreground" Value="#e0e0e0"/>
                </MultiTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Bd" Property="Background" Value="#333"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        <Style TargetType="TreeViewItem">
            <Setter Property="Template" Value="{StaticResource TreeViewItemTemplate}"/>
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu x:Name="Menu" Grid.Row="0" Background="#2a2a2a" Foreground="#e0e0e0">
            <MenuItem Header="_Archivo" Background="Transparent">
                <MenuItem Header="_Abrir JSON..." Click="OnOpenJson"/>
                <Separator/>
                <MenuItem Header="Re_cargar canales" Click="OnReload"/>
                <Separator/>
                <MenuItem Header="_Salir" Click="OnExit"/>
            </MenuItem>
            <MenuItem Header="_Ver" Background="Transparent">
                <MenuItem Header="Mostrar _panel de canales" x:Name="MenuItemTogglePanel" IsCheckable="True" IsChecked="True" Click="OnTogglePanel"/>
                <Separator/>
                <MenuItem Header="Siempre _visible durante transmisión" x:Name="MenuItemAlwaysOnTop"
                          IsCheckable="True" IsChecked="True" Click="OnAlwaysOnTopClick"/>
            </MenuItem>
            <MenuItem Header="_Ayuda" Background="Transparent">
                <MenuItem Header="_Acerca de..." Click="OnAbout"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftColumn" Width="300" MinWidth="0"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#242424" BorderBrush="#3a3a3a" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#2d2d2d" Padding="8,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Canales" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"
                                       Foreground="#e0e0e0"/>
                            <TextBlock Grid.Column="1" x:Name="ChannelCountText" FontSize="11"
                                       Foreground="#999" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1" Padding="6,4" Background="#242424">
                        <TextBox x:Name="SearchBox" Text="Buscar canales..." Style="{StaticResource SearchBoxStyle}"
                                 GotFocus="SearchBox_GotFocus" LostFocus="SearchBox_LostFocus" TextChanged="OnSearchTextChanged"/>
                    </Border>

                     <TreeView Grid.Row="2" x:Name="ChannelTreeView" Background="#242424" BorderThickness="0"
                               Foreground="#e0e0e0" SelectedItemChanged="OnChannelSelected"
                               KeyDown="OnChannelTreeViewKeyDown">
                        <TreeView.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#333"/>
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#e0e0e0"/>
                            <HierarchicalDataTemplate DataType="{x:Type models:ChannelGroup}"
                                                      ItemsSource="{Binding Samples}">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Margin="2,4"
                                           Foreground="#e0e0e0"/>
                            </HierarchicalDataTemplate>
                            <DataTemplate DataType="{x:Type models:Channel}">
                                <Grid Margin="24,1,2,1" ToolTip="{Binding Name}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="24"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Image Grid.Column="0" Width="20" Height="20" Source="{Binding Icono}" 
                                           Loaded="OnChannelIconLoaded" Margin="0,0,4,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                                               Foreground="#e0e0e0"/>
                                </Grid>
                            </DataTemplate>
                        </TreeView.Resources>
                    </TreeView>
                </Grid>
            </Border>

            <GridSplitter x:Name="GridSplitter" Grid.Column="1" Width="4" Background="#2a2a2a" 
                          ResizeBehavior="PreviousAndNext"/>

            <Border Grid.Column="2" Background="#111">
                <Grid>
                    <wv2:WebView2 x:Name="PlayerWebView" 
                                  CreationProperties="{x:Null}"
                                  CoreWebView2InitializationCompleted="OnWebViewInit"/>
                    <Border x:Name="WelcomeOverlay" Background="#1a1a1a">
                        <Grid>
                            <TextBlock Text="Seleccione un canal de la lista" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       FontSize="18" Foreground="#666"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <StatusBar x:Name="StatusBar" Grid.Row="2" Background="#2a2a2a" Foreground="#999" Padding="8,4" FontSize="11">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Text="Listo" Foreground="#999"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="ChannelStatusBlock" Foreground="#999"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
